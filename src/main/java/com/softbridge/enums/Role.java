package com.softbridge.enums;

/**
 * User roles in SoftBridge.
 * CLIENT  — external requester (Client A)
 * ADMIN   — internal decision maker (Client B)
 * DEVELOPER — developer assigned to projects
 */
public enum Role {
    CLIENT,
    ADMIN,
    DEVELOPER
}
